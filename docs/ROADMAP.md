# Дорожная карта развития

## Этап 1 — MVP ✅
- Почасовое расписание переговорки (свободно/занято).
- Форма бронирования с ФИО и Telegram.
- Раздел полезных ссылок.
- Веб-форма заявок по 1С CRM.

## Этап 2 — Портал сотрудника ✅
- Авторизация по пин-коду (сотрудник сам задаёт при регистрации).
- Профиль хранится на сервере (не зависит от браузера).
- Замена Telegram → Max (мессенджер компании).
- Бронирование на несколько часов (выбор интервала С–До).
- Клик по свободному слоту → popup записи.
- Перенос функционала crm-support-bot на сайт (ошибки/предложения 1С CRM).
- Telegram-уведомления админам при новых записях и заявках.
- Полезные ссылки в виде кнопок с иконками.
- Кнопка «Идея» — предложения по улучшению (сохранение + TG-уведомление).
- Админ-панель (отчёты): бронирования, CRM-заявки, идеи, пользователи.
- Дизайн в корпоративной палитре dxlc.ru, анимации, SVG-иконки.

## Этап 3 — Безопасность, ЛК и UX ✅
- 30-минутные слоты (вместо часовых).
- Отмена бронирований (полная и по слотам).
- Админ: редактирование данных пользователей (ФИО, контакт, пин).
- Админ: переключение роли админа из интерфейса.
- Мини личный кабинет сотрудника (ФИО, должность, контакт, дата регистрации).
- Загрузка фото профиля (drag&drop, прогресс-бар, валидация).
- Просмотр профиля коллеги по клику на имя в расписании.
- Аватары в шапке и в профиле.
- Безопасность: Helmet (CSP, HSTS, X-Frame-Options).
- Безопасность: Rate limiting (10 попыток/мин логин, 5/мин регистрация).
- Безопасность: Admin API переведён на POST (пин не в URL).
- Безопасность: ограничение длины строк на сервере.
- Подсказки для новых пользователей на странице регистрации.
- Анимация поезда на экране входа (терминальная площадка).
- Favicon (SVG, корпоративный стиль).
- Ссылки: Корп Алабуга вместо корп. портала.
- Домен lkds-room.ru + SSL-сертификат Let's Encrypt.
- Исправлена совместимость кнопок админки с CSP (event delegation).
- «Забыл пин-код» — запрос сохраняется на сервере, вкладка в админке с бейджем.
- CRM-форма очищена: только заявки по ошибкам 1С (предложения — через «Идея»).
- Telegram-уведомления строго для 1С CRM заявок.
- Полная мобильная адаптация (планшет, смартфон, iPhone notch).

## Этап 3.1 — ИТ-заявки ✅
- Нативный таб «ИТ-заявка» с пошаговым визардом (вместо ссылки на бота).
- Дерево категорий: ПО/установка, Компьютер/ноутбук, Интернет/сеть, ВКС (заблокирована), Другое.
- Подкатегории для каждой категории, свободный ввод для «Другое».
- Выбор локации: Модуль ЖД, Модуль КП, Офис 2 этаж, Диспетчерская и др.
- «Офис 2 этаж» → поле номера места (1–260).
- Хлебные крошки и кнопка «Назад» на каждом шаге.
- Сохранение заявок в data/it-tickets.json.
- Telegram-уведомление ИТ-админам (TG_IT_ADMIN_IDS).
- Админка: таб «ИТ-заявки» с таблицей (дата, категория, тип, локация, место, описание, кто).
- Ссылка на ИТ-бота убрана из раздела «Ссылки».
- Каркас готов к подключению API от ИТ-отдела (TODO-комментарий).

## Этап 4 — Рабочее место в профиле + ролевая модель

### Номер стола в профиле
- В ЛК сотрудника добавить поле «Рабочее место» (номер стола / локация).
- Сотрудник заполняет один раз при регистрации или в профиле.
- В ИТ-заявке автоматически подставляется локация и номер места из профиля.
- Шаги «Где возникла проблема?» и «Номер места» пропускаются, если данные уже в профиле (с возможностью изменить).

### Ролевая модель админки
- Роли вместо единого флага `isAdmin`:
  - **Админ ТИЭ** (суперадмин) — видит всё: бронирования, CRM, ИТ-заявки, идеи, пользователи.
  - **Админ ИТ** — видит только вкладку «ИТ-заявки» в админке, остальные вкладки скрыты.
  - **Админ проекта X** (будущее) — видит только свои вкладки проекта.
  - **Сотрудник** — без доступа к админке (как сейчас).
- Управление ролями: суперадмин назначает роли из интерфейса.
- Каждая роль видит только свой набор вкладок и данных.

### Синхронизация ролей с Telegram-ботами
- Единая база ролей: если пользователь — админ ИТ на портале, он автоматически админ в @LKDS_IT_bot и наоборот.
- При назначении роли на портале — обновляется роль в боте (через API бота).
- При назначении роли в боте — обновляется роль на портале (вебхук из бота).
- Привязка аккаунта Telegram к профилю на портале (по chat_id или username).

## Этап 5.1 — Синхронизация CRM-заявок портал → бот ✅
- CRM-заявка с портала автоматически записывается в `crm-support-bot/data/tickets.json` (формат бота).
- Админам отправляется TG-сообщение с inline-кнопкой «Взять в работу» (callback `take:{id}`).
- `message_id` сообщений сохраняются в тикет бота — кнопка работает штатно.
- Поле `source: "portal"` отличает заявки с портала от заявок из бота.
- Бот не модифицирован: обрабатывает `take:{id}` как обычно, заявки видны в `/admin` и отчёте.

## Этап 4.1 — ТЗ-трекер (замена Jira) ✅
- ТЗ-модуль: создание, редактирование, статусы, приоритеты, типы.
- Настраиваемые доски (Boards): CRUD, миграция, колонки, цвета.
- Пользовательские типы задач (Задача, Дефект, Заявка, Предложение, Недоработка).
- Исполнители (assignee), дедлайны, история изменений.
- Excel-экспорт/импорт ТЗ.
- Канбан-доска с drag-and-drop (HTML5 API, без библиотек).
- Kanban swimlanes: группировка по приоритету/исполнителю.
- Массовые операции (bulk): изменение статуса/приоритета для нескольких ТЗ.
- Комментарии к ТЗ: CRUD, уведомления подписчикам.
- Подписка на задачу (watch): TG-уведомления при смене статуса и новых комментариях.
- Связи между задачами (linked_tz_ids): добавление/удаление, навигация.
- Шаблоны ТЗ: CRUD, dropdown «Из шаблона» при создании, сохранение из формы.
- Утверждение предложений (approve workflow).
- AI-агент: промпт → выполнение → результат (через ai-worker.js).
- Умные дедлайны: автоуведомления, флаги overdue/deadline_soon/missing_deadline.
- Утренний дайджест ТЗ в Telegram (ai-worker.js).

## Этап 4.2 — База знаний (замена Confluence) ✅
- Wiki-модуль: категории, статьи, WYSIWYG-редактор (Quill v2).
- Загрузка изображений в статьи (multer, до 5 МБ).
- Читаемые slug-URL (`#w/категория/статья`).
- Доступ для всех авторизованных, редактирование — суперадмин.

## Этап 4.3 — Поиск и метрики ✅
- Глобальный поиск: GET /api/search по ТЗ, Wiki, ИТ-заявкам + topbar search с автодополнением.
- Метрики и дашборд (Chart.js): созданные ТЗ по неделям, распределение по статусам/приоритетам/типам, средний цикл.

## Этап 4.4 — Безопасность и оптимизация ✅
- PIN-коды хешируются через scrypt (миграция при старте).
- DOMPurify для XSS-защиты в Wiki.
- UUID-валидация через app.param().
- CSRF: проверка Origin-заголовка.
- Rate limiting на admin-эндпоинты (120 req/мин).
- Оптимизация filterTz(): фильтрация до computeTzFlags().
- Кэширование getUserByPin() (5 мин TTL).
- Утечки event listener: module-scope таймеры, destroy Quill.
- PIN не возвращается в ответе /api/auth/login.

## Этап 4.5 — Контроль доступа, онбординг и UX-фиксы ✅
- Переименование переговорок: Квадратный стол 1, Квадратный стол 2 (Круглый стол без изменений).
- Фикс кэша PIN: профиль, смена роли и toggle-admin мгновенно инвалидируют кэш (ранее — ждали 5 мин TTL).
- Кнопка «Отчёты» (админ-панель) видна только суперадминам (ранее — всем adminRole).
- Вкладки ТЗ, AI, Метрики — только суперадмины (было корректно, подтверждено тестами).
- Уведомления о просроченных ТЗ — только суперадмины (было корректно, подтверждено тестами).
- База знаний — доступна всем авторизованным (без изменений).
- Онбординг для новых сотрудников: модальное окно при первом входе с описанием доступных функций (переговорки, CRM-заявки, база знаний, ссылки). Показывается 1 раз, запоминается в localStorage.
- 13 автотестов API: контроль доступа для сотрудника, суперадмина, без PIN — все пройдены.

## Этап 4.6 — Исправления, связи ТЗ, канбан ✅

### Фаза A — Критичные фиксы ✅
- PIN убран из bookings.json — теперь хранится userId (владелец определяется по UUID).
- Atomic writes: все JSON-файлы записываются через tmp + rename.
- Удалён мёртвый код renderTzLinks() (был дубликат renderTzLinkedTasks).
- Метрики восстановлены из закомментированного состояния (вкладка видна суперадминам).

### Фаза B — Связи между ТЗ (linked_tz_ids) ✅
- Двусторонние связи: при добавлении A→B автоматически добавляется B→A, при удалении — симметрично.
- Связанные ТЗ загружаются с сервера (linked_tz_resolved) — не зависят от state.tzList и фильтров.
- Поиск при добавлении связи: select заменён на input с автодополнением (через /api/search).
- Добавление/удаление связей записывается в tz-history.

### Фаза C — Канбан улучшения ✅
- DnD работает в swimlanes (initKanbanDnD после рендера).
- Ближайший дедлайн и оставшиеся дни показываются на карточке канбана.
- HTML карточки вынесен в общую функцию kanbanCardHtml() — убрано дублирование.
- WIP-лимиты: настраиваемое поле wip_limit в колонках, визуальное предупреждение при превышении.
- Общие helpers: kanbanResolveDisplay() для displayOrder/colorMap.

### Фаза D — Техдолг ✅
- Валидация URL в link_confluence/link_jira: только http(s) URL принимаются.
- Rate limiting (30 req/мин) на /api/tz, /api/bookings, /api/tickets, /api/it-tickets, /api/suggestions.
- Audit log (data/audit-log.json): логируются update-user, toggle-admin, set-role, delete-board.
- Удалён мёртвый код обновления PIN в bookings при смене пароля.

## Этап 4.7 — Канбан: исправления и доработки

### Баги
1. **DnD не работает в swimlanes.** `initKanbanDnD()` ищет `.kanban-board`, но swimlanes рендерят `.kanban-swimlane-board` → drag-and-drop молча не работает.
2. **`board.columns.sort()` мутирует in-memory данные.** В `/api/admin/tz-kanban` вызывается `board.columns.sort(...)` без `.slice()` → мутирует объект из readJson, может повлиять на параллельные запросы.
3. **Карточки с удалённым статусом пропадают.** Если колонку удалили, а карточка осталась со старым статусом — она не попадает в `displayOrder` и исчезает с доски без следа.

### UX-доработки
4. **Swimlanes: нет WIP-лимитов.** В обычном канбане WIP-лимиты отображаются (красная рамка, счётчик `N/limit`), в swimlanes — нет.
5. **Swimlanes: нет счётчика карточек в хедере.** Обычный канбан показывает `cards.length/wipLimit`, swimlane хедер — только название.
6. **Дедлайн на карточке: берёт первый непустой, а не ближайший к фазе.** `tz.deadline || date_analysis_deadline || ...` — если задача в "Разработке", показывает общий дедлайн, хотя `date_dev_deadline` точнее.
7. **Нет drag-and-drop на мобильных.** HTML5 Drag API не работает на touch-устройствах — канбан полностью не интерактивен на телефонах/планшетах.

## Этап 5 — Единая система заявок (портал ↔ боты)

> **Проблема сейчас:** заявка, созданная на сайте, не попадает в отчёт бота
> и не может быть взята в работу через бота. Портал и боты — изолированные системы.
>
> **Частично решено:** этап 5.1 — CRM-заявки с портала синхронизируются в бот (запись в файл + TG-кнопка).

### Принцип: один источник правды
- Портал ЛКДС становится **единым хранилищем** всех заявок (CRM, ИТ, будущие).
- Боты (CRM, ИТ и др.) работают через **API портала**, а не через своё локальное хранилище.
- Заявка, созданная где угодно (сайт, бот, API), видна везде одинаково.

### CRM-заявки (портал + @crm_support_bot)
- Бот при создании заявки вызывает `POST /api/tickets` портала (вместо записи в свой JSON/БД).
- Бот при команде «Отчёт» вызывает `GET /api/tickets` портала — получает все заявки (и с сайта, и из бота).
- Бот при команде «Взять в работу» вызывает `PATCH /api/tickets/:id` портала — статус обновляется в единой базе.
- На портале в админке отображаются статусы: новая → в работе → закрыта (единые с ботом).

### ИТ-заявки (портал + @LKDS_IT_bot)
- Аналогично CRM: бот работает через API портала.
- `POST /api/it-tickets` — создание, `GET /api/it-tickets` — отчёт, `PATCH /api/it-tickets/:id` — смена статуса.
- Все заявки (с сайта и из бота) в одном месте.

### Универсальный шаблон для будущих ботов
- Любой новый бот/модуль (заявки по АХО, заявки на транспорт и т.д.) подключается по тому же API-паттерну.
- Эндпоинты: `POST /api/{тип}-tickets`, `GET /api/{тип}-tickets`, `PATCH /api/{тип}-tickets/:id`.
- Ролевая модель (этап 4) определяет, кто видит какие заявки.

### API-контракт для ботов
- `POST /api/tickets` — создать заявку (поля: source = 'bot'|'portal', tgChatId, ...).
- `GET /api/tickets?status=new` — список заявок с фильтрацией.
- `PATCH /api/tickets/:id` — обновить статус, назначить исполнителя.
- Аутентификация ботов: API-ключ (`X-Bot-Token`) или по TG chat_id.
- Поле `source` в заявке — откуда пришла (портал / бот / API).

## Этап 6 — Удобство и контроль
- Фильтры по неделе/месяцу в расписании.
- Валидация бизнес-правил (мин/макс длительность, лимит на день).
- Статусы заявок (новая → в работе → закрыта) — единые для портала и ботов.
- Excel-экспорт отчётов из админки.
- Добавление новых переговорок через админку.

## Этап 7 — Интеграции
- Уведомления в Max (когда API станет доступен).
- SMTP-уведомления на почту (предложения, CRM-заявки).
- Вебхуки в корпоративные системы (1С, CRM).
- Синхронизация с календарями (iCal).

## Этап 7.1 — Масштабирование
- Переход с JSON на PostgreSQL.
- История изменений и аудит действий.
- Правила бронирования для разных комнат.

## Этап 8 — Аналитика
- Дашборд загрузки переговорок (по дням/часам).
- Коэффициент утилизации и популярные слоты.
- SLA по заявкам 1С CRM.
- Статистика по сотрудникам.

---

## Замечания по результатам аудита (2026-02-26)

### 🔴 Критичные — исправить до этапа 4

1. ~~**PIN хранится в открытом виде.**~~ ✅
   Исправлено: PIN хешируется через scrypt. Миграция при старте автоматически конвертирует старый формат.

2. ~~**PIN в URL при удалении брони.**~~ ✅
   Исправлено: `POST /api/bookings/:id/cancel` с пином в теле запроса.

3. ~~**Race condition на JSON-файлах.**~~ ✅
   Исправлено: in-memory mutex (`withLock`) для критичных read-modify-write операций (регистрация, бронирования). Долгосрочно: PostgreSQL (этап 7.1).

4. ~~**Захардкоженные Telegram admin IDs в исходниках.**~~ ✅
   Исправлено: `CRM_BOT_ADMIN_IDS` читается из `.env` (с fallback на `TG_ADMIN_IDS`).

### 🟡 Баги — исправить в ближайшем релизе

5. ~~**Баг с аватарами: клиент всегда запрашивает `.jpg`.**~~ ✅
   Исправлено: сервер возвращает реальное имя файла, клиент использует его.

6. ~~**IT-конфиг продублирован в клиенте и сервере.**~~ ✅
   Исправлено: клиент загружает конфиг из `/api/it-config`. Хардкод удалён.

7. ~~**Мёртвый код в ИТ-визарде.**~~ ✅
   Исправлено: убрана мёртвая ветка if/else.

### 🟢 Техдолг — включить в ближайшие этапы

8. ~~**`crm-requests.json` — мёртвый файл.**~~ ✅
   Исправлено: файл удалён.

9. ~~**`PLAN.md` устарел.**~~ ✅
   Исправлено: файл удалён, `ROADMAP.md` — единственный источник.

10. ~~**Два этапа с номером 7.**~~ ✅
    Исправлено: «Масштабирование» переименован в Этап 7.1.

11. ~~**Нет CSRF-защиты.**~~ ✅
    Исправлено: middleware проверяет `Origin` заголовок на мутирующих запросах.

12. **Запись за пределы проекта.**
    `CRM_BOT_TICKETS_FILE` пишет в `../crm-support-bot/data/tickets.json`. Хрупкая файловая зависимость без fallback.
    → Учесть при переходе на единый API (этап 5): убрать прямую запись в файлы бота.

13. ~~**`alert()` в админке.**~~ ✅
    Исправлено: заменены на inline-уведомления `adminMsg()`.

14. ~~**Нет пагинации и поиска в таблицах админки.**~~ ✅
    Исправлено: пагинация (50 записей/стр), глобальный поиск, фильтры в ТЗ-трекере.

15. ~~**Таб «ИТ-заявка» помечен «В разработке», хотя функционал рабочий.**~~ ✅
    Исправлено: ярлык убран.
